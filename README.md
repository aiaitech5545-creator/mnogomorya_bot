# mnogomorya_bot (Railway)

Телеграм-бот для публикации постов в канале **@mnogomorya** с поддержкой:
- черновиков (текст + медиа),
- альбомов (2–10 фото/видео в одном посте),
- отложенной публикации по таймеру.

Админ: user_id `211779388` (зашито в коде).

## Структура проекта

- `app.py` — код бота
- `requirements.txt` — зависимости
- `Procfile` — тип процесса для Railway (`worker: python app.py`)
- `.env.example` — пример переменных окружения

## Деплой на Railway

1. Создай репозиторий на GitHub и загрузите туда эти файлы.
2. На https://railway.app создай новый проект → Deploy from GitHub → выбери репозиторий.
3. В Settings → Variables добавь переменную:
   - `TELEGRAM_BOT_TOKEN` = токен из @BotFather
4. Убедись, что Railway определил тип сервиса как **Worker** (если нет — поменяй вручную).
5. Стартовая команда: `python app.py` (Railway возьмёт её из Procfile, но можно указать явно).
6. Нажми Deploy и смотри логи — там будет `Bot started ...`, если всё ок.

## Использование

### Основное
- Напишите боту `/start` — он пришлёт подсказку.
- Отправьте боту текст — он станет текстом черновика.
- Отправьте 2–10 фото/видео подряд — бот соберёт альбом.
- Под ответом бота будут кнопки:
  - `📄 Предпросмотр` — покажет, как пост выглядит,
  - `📢 Опубликовать` — отправит пост в канал `@mnogomorya`,
  - `🗑 Очистить` — сбросит черновик и отменит таймер.

### Таймер

Поддерживаются три формата команды `/timer`:

- `/timer HH:MM` — публикация сегодня в указанное время (если оно уже прошло — завтра).
- `/timer YYYY-MM-DD HH:MM` — публикация в конкретный день и время.
- `/timer in 10m` / `/timer in 2h` / `/timer in 1d` — отложить на N минут/часов/дней.

Дополнительные команды:

- `/when` — показать, на когда сейчас стоит публикация.
- `/cancel_timer` — отменить отложенную публикацию.
- `/whoami` — показать ваш Telegram user_id.

> Важно: управлять ботом может только админ (`ADMIN_USER_ID=211779388`).

